# build endpoint dataset
`python src/build_shuffled_endpoint_dataset_metadata.py --config-name=build_shuffled_endpoint_dataset.yaml`

Use the build_shuffled_endpoint_dataset.yaml, to change the source , destination folder and other configurations. 

