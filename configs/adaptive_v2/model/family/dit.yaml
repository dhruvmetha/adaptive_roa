# @package model

_target_: adaptive_roa.model.dit_flow.DiTFlowModel

embedded_dim: ${model_dims.embedded_dim}
latent_dim: ${flow_matching.latent_dim}
condition_dim: ${model_dims.condition_dim}
time_emb_dim: ${model_dims.time_emb_dim}
output_dim: ${model_dims.output_dim}
hidden_dim: ${model_dims.dit_hidden_dim}
num_blocks: ${model_dims.dit_num_blocks}
num_heads: ${model_dims.dit_num_heads}
cond_dim: ${model_dims.dit_cond_dim}
mlp_ratio: ${model_dims.dit_mlp_ratio}
dropout: ${model_dims.dit_dropout}
