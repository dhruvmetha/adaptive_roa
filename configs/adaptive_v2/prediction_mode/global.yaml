# @package _global_
# Global prediction mode (default): direct endpoint prediction via flow matching
# No overrides needed — system configs already define model + flow_matcher for global mode
prediction_mode: global
