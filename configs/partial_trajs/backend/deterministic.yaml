# @package _global_
# Deterministic backend: T-step MLP regressor (DynamicsRegressor).
# Hyperparameters mirror the adaptive classifier runs (predictor/classifier.yaml):
# 200 epochs, lr 1e-3, dropout 0.
backend: deterministic
batch_size: 1024
max_epochs: 200
lr: 1.0e-3
dropout: 0.0
