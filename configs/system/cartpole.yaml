# @package _global_.system
# =====================================================================
# CartPole System Configuration
# =====================================================================
# CartPole dynamics with ℝ²×S¹×ℝ manifold structure
# State: (x, θ, ẋ, θ̇) where:
#   - x ∈ ℝ (cart position)
#   - θ ∈ S¹ (pole angle)
#   - ẋ ∈ ℝ (cart velocity)
#   - θ̇ ∈ ℝ (pole angular velocity)
# =====================================================================

_target_: adaptive_roa.systems.cartpole.CartPoleSystem

# Dataset directory containing dataset_description.json with bounds
# Default: use data_dir resolver if available, else hardcoded fallback
dataset_dir: ${data_dir:/common/users/shared/pracsys/genMoPlan/data_trajectories}/deterministic/cartpole_pybullet
